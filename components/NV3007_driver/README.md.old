# NV3007 LCD 驱动库

面向 ESP32-P4 平台的 NV3007 芯片 SPI 屏幕驱动组件。

## 目录

- [特性](#特性)
- [架构](#架构)
- [快速开始](#快速开始)
- [API 参考](#api-参考)
- [引脚配置](#引脚配置)
- [性能数据](#性能数据)
- [已知限制](#已知限制)

## 特性

- **硬件 SPI + DMA** — 基于 ESP-IDF SPI 主模式驱动，80 MHz 时钟，零 CPU 等待大块传输
- **SPI Mode 0** — CPOL=0、CPHA=0，严格对齐 NV3007 数据手册 §4.1.1.1 时序
- **软件 CS** — 通过 `pre_cb`/`post_cb` 回调实现，避免 IO MUX 受限引脚被硬件 CS 绑定
- **完整绘图 API** — 点、线、矩形、圆、三角形、位图显示
- **ASCII 字库** — 内置 6×12 / 8×16 / 12×24 三种字号
- **RGB565 16 位色** — 针对 142×428 竖屏（默认方向 1，可切换横/竖/旋转）

## 架构

```
┌────────────────────────────────────────────┐
│  NV3007 API (公共接口)                      │
│  DrawPoint / DrawLine / Fill / ShowString… │
├────────────────────────────────────────────┤
│  纹理层                                     │
│  Fill 分大小路径:                           │
│    >32 px → DMA 块传输 (10行缓冲复用)        │
│    ≤32 px → 逐像素 WriteHalfWord            │
├────────────────────────────────────────────┤
│  SPI 传输层                                 │
│  spi_device_polling_transmit                │
│  pre_cb → CS↓   post_cb → CS↑              │
│  flags = HALFDUPLEX (只发不收)              │
├────────────────────────────────────────────┤
│  ESP32-P4 SPI2 外设 (@ 80 MHz)              │
│  DMA → GDMA 通道自动搬运                     │
└────────────────────────────────────────────┘
```

### 关键加速原语

| 原语 | 策略 | 加速比 |
|------|------|--------|
| `Fill` (大区域) | 预填充 10 行缓冲区 → DMA 分块循环发送 | ~100× vs 逐像素 |
| `FillHLine` (水平线段) | 一条 SetWindow + DMA 整行，渐变刷新核心 | ~100× vs 逐点 |
| `DrawLine` (水平/垂直线) | 自动短路到 `FillHLine`/`Fill`，不走 Bresenham | 瞬间完成 |
| `ShowChar` (非叠加) | 行缓冲: 每行像素一次 SPI 传输 | ~8× vs 逐像素 |

## 快速开始

### 1. 添加组件

```cmake
# 顶层 CMakeLists.txt 中确保 EXTRA_COMPONENT_DIRS 包含组件路径
set(EXTRA_COMPONENT_DIRS "components")
```

### 2. 引入头文件

```c
#include "NV3007_driver.h"

void app_main(void) {
    NV3007_Init();                            // 初始化屏幕
    NV3007_FastFill(BLACK);                   // 清屏为黑色
    NV3007_ShowString(10, 20, "Hello!",       // 显示文字
                      WHITE, BLACK, 16, 0);
}
```

### 3. 配置引脚

在 `NV3007_driver.h` 中修改宏，或编译时通过 `-D` 覆盖：

```c
#define NV3007_PIN_MOSI   GPIO_NUM_20
#define NV3007_PIN_CLK    GPIO_NUM_21
#define NV3007_PIN_CS     GPIO_NUM_22
#define NV3007_PIN_DC     GPIO_NUM_4
#define NV3007_PIN_RST    GPIO_NUM_5
#define NV3007_PIN_BLK    GPIO_NUM_6
```

## API 参考

### 初始化 & 反初始化

| 函数 | 说明 |
|------|------|
| `NV3007_Init(void)` | 初始化 SPI 总线、GPIO、发送寄存器序列 |
| `NV3007_Deinit(void)` | 进入睡眠、释放 SPI 总线 |

### 绘图

| 函数 | 说明 |
|------|------|
| `NV3007_DrawPoint(x, y, color)` | 画一个像素 |
| `NV3007_DrawLine(xs, ys, xe, ye, color)` | 画直线（水平/垂直自动走快速路径） |
| `NV3007_FillHLine(x, y, w, color)` | 快速水平线填充（一条 SPI 传输） |
| `NV3007_Fill(xs, ys, xe, ye, color)` | 区域填充（大区域 DMA 加速） |
| `NV3007_FastFill(color)` | 全屏填充 |
| `NV3007_DrawRectangle(xs, ys, xe, ye, color)` | 空心矩形 |
| `NV3007_DrawFillRectangle(xs, ys, xe, ye, color)` | 实心矩形 |
| `NV3007_DrawCircle(xc, yc, r, color, mode)` | 圆（mode: 0 空心, 1 实心） |
| `NV3007_DrawTriangle(x,y,xs,ys,xe,ye,color)` | 空心三角形 |

### 文字

| 函数 | 说明 |
|------|------|
| `NV3007_ShowChar(x, y, ch, fc, bc, size, mode)` | 单字符（size: 12/16/24, mode: 0 覆盖背景, 1 叠加） |
| `NV3007_ShowString(x, y, str, fc, bc, size, mode)` | 字符串 |
| `NV3007_ShowStringCenter(y, str, fc, bc, size, mode)` | 水平居中字符串 |
| `NV3007_ShowNum(x, y, num, len, fc, bc, size, mode)` | 整数 |
| `NV3007_ShowFloatNum(x, y, f, prec, len, fc, bc, size, mode)` | 浮点数 |
| `NV3007_ShowPicture(x, y, w, h, pic)` | RGB565 位图 |

### 颜色

| 宏 | 值 | 宏 | 值 |
|----|-----|----|-----|
| `BLACK` | `0x0000` | `WHITE` | `0xFFFF` |
| `RED` | `0xF800` | `GREEN` | `0x07E0` |
| `BLUE` | `0x001F` | `YELLOW` | `0xFFE0` |
| `CYAN` | `0x7FFF` | `MAGENTA` | `0xF81F` |
| `GRAY` | `0x8430` | `BROWN` | `0xBC40` |

## 引脚配置

所有引脚宏均可通过 `#ifndef` 机制在组件外覆盖。默认值：

| 信号 | GPIO | 方向 |
|------|------|------|
| MOSI (SDA) | 20 | 输出 |
| CLK (SCL) | 21 | 输出 |
| CS | 22 | 输出（软件控制） |
| DC | 4 | 输出 |
| RST | 5 | 输出 |
| BLK | 6 | 输出 |

> **注意:** 选脚时避开 ESP32-P4 的 Flash/PSRAM 复用脚（26-31），确保引脚是普通 GPIO。

## 性能数据

以下数据在 ESP32-P4 @ 360 MHz、SPI 80 MHz 下测得：

| 操作 | 耗时 | 说明 |
|------|------|------|
| 全屏填充 | ~15 ms | 142×428×2 = 121 KB, DMA 分块 |
| 渐变 428 行 | ~30 ms | 每行一次 FillHLine |
| 16pt 字符 | ~80 µs | 行缓冲, 每行一次 SPI |
| 水平线 (142px) | ~300 µs | SetWindow + DMA |

## 已知限制

- 中文字库已移除（RISC-V 编译器不兼容多字节字面量），需用户自行添加
- `ShowChar` 叠加模式（mode=1）性能较低，建议用非叠加模式
- 组件依赖 `esp_driver_spi`，ESP-IDF ≥ 5.0
