# UVC Capture Card Driver

基于 ESP-IDF USB Host UVC 的**通用采集卡驱动**，将 USB 摄像头 / HDMI 采集卡的视频帧捕获并保存到 SD 卡。

> 本组件集成了 USB Host → UVC 协议栈 → ISOC/Bulk 传输 → MJPEG 帧解码 → SD 卡写入的完整流水线。

---

## 架构

```
┌─────────────────────────────────────────────────┐
│                    main.c                       │
│  (设备配置: VID/PID, 分辨率, 格式, 帧率)          │
└────────────────────┬────────────────────────────┘
                     │ uvc_capture_init(&stream_cfg)
┌────────────────────▼────────────────────────────┐
│          uvc_capture_card_driver                │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │ USB Host │→ │ UVC 协议 │→ │ 帧捕获 & 缓存  │  │
│  │ 初始化   │  │ 枚举/控制 │  │ (SPIRAM)      │  │
│  └──────────┘  └──────────┘  └───────┬───────┘  │
│                        ┌─────────────▼────────┐ │
│                        │   sdmmc_driver       │ │
│                        │   (SD 卡写入)         │ │
│                        └──────────────────────┘ │
└─────────────────────────────────────────────────┘
```

**层次分明**：`main.c` 只管设备参数 → `uvc_capture_card_driver` 管流水线 → `sdmmc_driver` 管存储。

---

## 功能

| 功能 | 说明 |
|---|---|
| 通用 API | 接受任意 `uvc_host_stream_config_t`，不绑定特定芯片 |
| 自动初始化 | 一次调用完成 USB → UVC → SD 卡全链路初始化 |
| 持续流 | 启动后保持 UVC 流常开，避免 HDMI 接收器重新训练 |
| 按需捕获 | `uvc_capture_one_frame()` 阻塞等待下一帧 |
| MJPEG 修复 | 自动裁剪 SOI (0xFF 0xD8) 前的垃圾字节 |
| 缺 EoF 修复 | MS2109 等无 EoF 标志的芯片自动补全帧 |
| SPIRAM 缓冲 | 帧缓冲分配在 SPIRAM，不占用内部 DRAM |
| SD 卡存储 | 通过 `sdmmc_driver` 以 FAT 文件系统写入 |

---

## 文件结构

```
components/uvc_capture_card_driver/
├── CMakeLists.txt
├── idf_component.yml                ← 组件依赖（usb: ^1.0.0）
├── Kconfig                          ← UVC 调试选项
├── uvc_capture_card_driver.c        ← 核心流水线
├── uvc_host.c                       ← USB Host UVC 驱动（集成）
├── uvc_isoc.c                       ← ISOC 等时传输（含 MS2109 修复）
├── uvc_bulk.c                       ← Bulk 传输
├── uvc_control.c                    ← UVC 控制传输
├── uvc_descriptor_parsing.c         ← UVC 描述符解析
├── uvc_descriptor_printing.c        ← UVC 描述符打印
├── uvc_frame.c                      ← 帧缓冲管理
├── include/
│   ├── uvc_capture_card_driver.h    ← 公共 API
│   ├── usb/uvc_host.h               ← UVC Host 公开接口
│   └── esp_private/                 ← 内部头文件
└── private_include/                 ← 私有头文件
```

---

## 快速开始

### 1. 依赖

在你的项目 `CMakeLists.txt` 中：

```cmake
idf_component_register(SRCS "main.c"
                       INCLUDE_DIRS "."
                       REQUIRES uvc_capture_card_driver)
```

### 2. 最小示例（MS2109 采集卡，每 10 秒抓一帧）

```c
#include "uvc_capture_card_driver.h"
#include "freertos/FreeRTOS.h"
#include "freertos/task.h"

void app_main(void)
{
    /* ── 设备配置：MS2109 HDMI 采集卡 ── */
    const uvc_host_stream_config_t s_ms2109_cfg = {
        .usb = {
            .vid  = 0x534D,
            .pid  = 0x2109,
            .dev_addr = UVC_HOST_ANY_DEV_ADDR,
        },
        .vs_format = {
            .format = UVC_VS_FORMAT_MJPEG,
            .h_res  = 1920,
            .v_res  = 1080,
            .fps    = 30,
        },
        .frame_cb        = NULL,    /* 由驱动内部处理 */
        .stream_event_cb = NULL,
        .frame_size      = 1024 * 1024, /* 1 MB / 帧 */
        .frame_buffer_num = 6,
        .urb_num         = 4,
        .urb_size        = 0,       /* 自动 (4×MPS) */
        .buffer_flags    = UVC_HOST_BUF_SPIRAM
                         | UVC_HOST_BUF_DMA_ALIGNED,
    };

    /* ── 一键初始化（USB + UVC + SD 卡） ── */
    ESP_ERROR_CHECK(uvc_capture_init(&s_ms2109_cfg));

    int count = 0;
    while (1) {
        uint8_t *frame = NULL;
        size_t   len   = 0;

        if (uvc_capture_one_frame(&frame, &len, pdMS_TO_TICKS(5000)) == ESP_OK) {
            char name[32];
            snprintf(name, sizeof(name), "cap_%04d.jpg", count++);
            uvc_capture_save_to_sd(name, frame, len);
            free(frame);
        }

        vTaskDelay(pdMS_TO_TICKS(10000));  /* 每 10 秒 */
    }
}
```

### 3. 适配其他采集卡

只需修改 `uvc_host_stream_config_t` 中的 `vid`、`pid`、`format`、`h_res`、`v_res`、`fps` 即可：

```c
/* UVC 标准摄像头 (YUY2) */
const uvc_host_stream_config_t my_camera = {
    .usb.vid = 0x0BDA, .usb.pid = 0x3030,    /* 替换为你的 VID/PID */
    .vs_format = {
        .format = UVC_VS_FORMAT_YUY2,
        .h_res  = 640,
        .v_res  = 480,
        .fps    = 30,
    },
    .frame_size      = 640 * 480 * 2,   /* YUY2: 2 bytes/pixel */
    .frame_buffer_num = 4,
    .urb_num         = 4,
    .buffer_flags    = UVC_HOST_BUF_SPIRAM,
};

ESP_ERROR_CHECK(uvc_capture_init(&my_camera));
```

---

## API 参考

### `uvc_capture_init()`

```c
esp_err_t uvc_capture_init(const uvc_host_stream_config_t *stream_cfg);
```

按顺序执行：
1. SD 卡初始化 + FAT 挂载
2. USB Host Library 安装 + 事件任务
3. UVC Host 驱动安装
4. 打开 UVC 流
5. 启动持续流（管道保持预热）
6. 丢弃前 3 帧（HDMI 接收器热身）

### `uvc_capture_one_frame()`

```c
esp_err_t uvc_capture_one_frame(uint8_t **out_data, size_t *out_len,
                                uint32_t timeout_ms);
```

阻塞等待下一帧。数据分配在 SPIRAM，调用者负责 `free()`。
对于 MJPEG，SOI 前的垃圾字节会自动裁剪。

### `uvc_capture_save_to_sd()`

```c
esp_err_t uvc_capture_save_to_sd(const char *filename,
                                 const uint8_t *data, size_t len);
```

将二进制数据写入 SD 卡。路径相对于 SD 卡根目录。

### `uvc_capture_deinit()`

```c
esp_err_t uvc_capture_deinit(void);
```

停止流 → 关闭流 → 卸载 UVC → 卸载 USB Host → 卸载 SD 卡。

---

## Kconfig 选项

`menuconfig` → **USB HOST UVC**：

| 符号 | 默认值 | 说明 |
|---|---|---|
| `CONFIG_UVC_PRINTF_CONFIGURATION_DESCRIPTOR` | n | 打印 UVC 配置描述符 |
| `CONFIG_UVC_INTERVAL_ARRAY_SIZE` | 3 | 帧间隔数组大小 |
| `CONFIG_UVC_CHECK_PAYLOAD_HEADER_EOH` | y | 检查 EOH 位 |

---

## 硬件需求

| 项目 | 要求 |
|---|---|
| 芯片 | ESP32-P4（需 SPIRAM） |
| USB | USB Host 端口（外接 MS2109 / 摄像头） |
| SD 卡 | 通过 SDMMC 接口连接 |
| 上拉电阻 | CMD、D0~D3 各接 10 kΩ 到 3.3V |

---

## MS2109 深度分析

MS2109 (VID=0x534D, PID=0x2109) 是一款 MacroSilicon HDMI→USB 采集芯片。经过在 Linux 平台上用 `yavta`、`v4l2-ctl` 等工具全面测试，得出以下结论：

### 🟢 好消息：完全标准的 UVC 设备

MS2109 **是一颗完全标准的 UVC 1.0 设备**，不需要任何私有驱动或 HID 命令来控制分辨率/帧率。

- ✅ 支持标准 UVC **VS_PROBE_CONTROL / VS_COMMIT_CONTROL** 动态切换
- ✅ 无需 HID 接口即可切换分辨率、格式、帧率
- ✅ 所有切换都通过标准 UVC Video Streaming Interface 完成

### 📊 支持的格式与分辨率

**MJPEG 格式（推荐使用）：**

| 分辨率 | 最高帧率 |
|---|---|
| 1920×1080 | **60 fps** ⚡ |
| 1600×1200 | 60 fps |
| 1360×768 | 60 fps |
| 1280×1024 | 60 fps |
| 1280×960 | 60 fps |
| 1280×720 | 60 fps |
| 1024×768 | 60 fps |
| 800×600 | 60 fps |
| 720×576 | 60 fps |
| 720×480 | 60 fps |
| 640×480 | 60 fps |

> **注意**：不同批次 / 固件版本的 MS2109，能力可能不同。有用户的芯片只能到 1080p30，本文测试的芯片支持 1080p60。

**YUYV (未压缩) 格式：**

| 分辨率 | 帧率 |
|---|---|
| 全部 | 最多 5 fps |

YUYV 受 USB 2.0 带宽限制（1080p YUYV ≈ 125 MB/s，远超 480 Mbps ≈ 60 MB/s），几乎没有实用价值。**建议统一使用 MJPEG。**

### 🔴 关键发现：EDID 固定为 1080p

MS2109 的 HDMI EDID 被芯片内部**硬编码为 1080p**。这意味着：

- 🖥️ HDMI 源设备（电脑/游戏机）看到的始终是"1080p 显示器"
- 无论你在 UVC 层面设置什么分辨率，HDMI 源输出的始终是 **1920×1080**
- 设置 720p / 640×480 等低分辨率时，MS2109 芯片内部做**硬件缩放**（1080p → 目标分辨率），而非要求 HDMI 源改变输出

**实测验证**：在 Linux 上通过 UVC 设置 720p，HDMI 源（Windows 电脑）的显示设置仍显示输出为 1920×1080@60Hz，证实 EDID 不随 UVC 分辨率设置改变。

### ✅ 最佳实践

| 建议 | 原因 |
|---|---|
| **始终使用 1080p** | 低分辨率只是硬件缩放，不会降低 HDMI 源负载，只会降低图像质量 |
| **使用 MJPEG 格式** | YUYV 受带宽限制无实用价值 |
| **使用 60 fps**（如芯片支持） | 流畅度显著提升，带宽允许 |
| **预计 8-16 Mbps** | MJPEG 典型码率，单帧约 100-200 KB |

### 🛠️ 驱动内置修复

MS2109 固件存在以下缺陷，驱动已内置修复：

| 问题 | 修复 |
|---|---|
| 不发送 EoF（帧结束）标志 | ISOC 层在下一帧 SoF 时自动补全当前帧 |
| MJPEG 帧头含垃圾字节 | 应用层自动搜索并裁剪 SOI |
| HDMI 接收器冷启动彩色偏移 | 持续流保持预热，丢弃前 3 帧 |

---

## 依赖

| 依赖 | 来源 |
|---|---|
| `sdmmc_driver` | 本地组件 |
| `usb` ^1.0.0 | ESP-IDF 组件管理器 |
| ESP-IDF ≥ 6.0 | FreeRTOS, SPIRAM, SDMMC, USB Host |

---

## 许可

基于 ESP-IDF 示例和 espressif/usb_host_uvc 修改。沿用 Apache 2.0 许可。
