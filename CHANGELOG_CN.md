# 更新日志

> **中文** | [English](CHANGELOG.md)

简洁记录改了什么、什么时候改的。这里的日期是真实日期，不是提交日期：ESP32-P4 那部分工作是在另一个工作区完成的，2026-09-12 才导入本仓库，所以有些提交带着九月的日期，而里面的工作本身更早。

## 2026-09 — 项目因更快、多模态的模型而重启

最初跑通的那一版受延迟制约：API 占据每一步的 64–91%，而一步的耗时大约等于它的输出 token 数除以模型吞吐。项目当时是被搁置而不是被优化，因为主导项不在我们手里。

现在重启，是因为当前的模型世代——DeepSeek V4.1 flash，`model_name: "deepseek-flash"`——让这个回路真正可用：输出吞吐高得多，并且原生支持视觉，省掉了“想看屏幕就得先 OCR 或先让模型描述图像”这一层。harness 本身没有为它做任何改动，这正是把模型当作一个藏在配置键后面的无状态函数的意义。

## 2026-09-12 — 定位说明与设备身份

### 新增

主 README 增加了“软件 agent 去不了的地方”：目标还没有可用的操作系统时、装不了软件的目标、以及目标机自己的网络就是故障本身时——最后这种是软件 agent 最需要它的时刻恰好掉线的情况，因为它正是通过那张坏掉的网去够模型的。

`hid_device`：USB 序列号改为由芯片 base MAC 推导（`VIC-xxxxxxxxxxxx`）。此前每台设备都报固定的 `0001`，主机日志只能说“有个 Victrl 接进来过”，永远说不清是哪一台。制造商（`Victrl`）、产品（`Victrl HID Bridge`）和 HID 接口（`Victrl HID`）字符串本来就有。

README 提到，基于摄像头**识别**屏幕（而不是采集信号）的版本可以连视频输出这条要求都去掉。那是方向，不是已实现的东西。

### 变更

README 不再声称目标察觉不到这个设备。目标是察觉得到的：设备会带着名称和每台唯一的序列号枚举出来，而且这是刻意的——一个无法被归属的外设就是隐蔽工具，而使用原则明确禁止隐蔽。目标做不到的是伸手进来；轨迹、计划、会话存储也从不落到目标的磁盘。

删掉了一处不成立的对比：“云端 agent 框架把记忆放在服务器上”对任何主流框架都不成立。云端只做推理——软件 agent 和本项目在这一点上没有区别——它们的 harness 和记忆都在本地运行。真正的区别是：软件 agent 需要一个通用操作系统和一条进入目标的软件路径，而这里 harness 和记忆都在目标之外的一块微控制器上。

## 2026-09-12 — 组件文档审计

### 修复

`wifi_manager`：链路一断就清空 STA 地址，因此 `wifi_manager_is_connected()`（读的是 `s_ip_addr`）在掉线后不再报告“已连接”，屏幕和 WebUI 也不再显示过期地址。

`wifi_manager`：SNTP 服务器名只会写进 `time_status_t.servers` 真正拥有的槽位，且 `n_servers` 不会超过实际拷贝的数量；把 `CONFIG_LWIP_SNTP_MAX_SERVERS` 调到 3 以上不会再写越界——`web_server.c` 正是按 `n_servers` 索引这个数组的。

`storage_manager`：合并最旧两条的内存分配失败时，`stm_add()` 不再写到短期记忆数组末尾之外，改为丢弃最旧一条。`strdup()` 失败时也不再存进一个 NULL 空洞——那会让每个用 `strlen()` 遍历的读者崩溃。

`storage_manager`：`meta.json` 在每步重写后仍保留 `created` 字段，会话不再在第一次更新步数时丢掉创建时间。

`agent_core`：“单步”现在真的只跑一次就停，并且跳过步间休眠。STEP 到 PAUSED 的转换原本不可达——`agent_should_continue()` 对 STEP 返回真——所以单步的表现和 RUN 完全一样，永远不会暂停。

`sdmmc_driver`：挂载失败的日志改成真实存在的 Kconfig 选项名（`CONFIG_SDMMC_DRV_FORMAT_IF_MOUNT_FAILED`）。

### 变更

12 个组件的 README 全部对照源码审计并重写；每个组件现在都有与 `README_CN.md` 并列的英文 `README.md`。审计纠正了与代码不符的说法，包括并不存在的 `jpeg_get_info()`、`ESP_COLOR_FOURCC_BGR24`、`sd_access_mutex`，JPEG 质量默认值（是 60 不是 25），以磅而非像素描述的字体，以及过期的 UVC 流结构字段。

根目录 README 增加了指向各组件文档的入口。

### 删除

三个过期的 `README.md.old`。

### 已记录但有意不改的问题

`NV3007_driver`：`F9/F2 fix` 段落里的 `NV3007_WriteByte(0x17)` 是一个没有前置寄存器写的裸数据字节，`0xF9` 从未被寻址——很可能是漏了 `C8(0xF9,0x17)`。但面板现在初始化正常、显示正常，所以这段时序保持原样。

`wifi_manager`：`time_status_t.failures` 暴露在 `/api/status` 里，却从来没有人给它赋值。lwIP 的 SNTP 没有失败回调，要给出真实数值需要一个“已启动但一直没同步”的看门狗判定。字段暂时保留，但请把 0 理解为“未测量”，而不是“没有失败”。

## 2026-09-11 — 输入法通道实验

数字小键盘的假设被证伪：在微软拼音上，小键盘数字和主键盘数字行一样会被当成候选选择键吃掉。实验记录在 `docs/技术文档.md`，包括它为什么拖了这么久才定论——必须让 `type` 动作的摘要开始记录它实际用的**通道**，才能把“小键盘也被吃了”和“根本没试小键盘”区分开。

## 2026-05-30 — 智能体回路跑在 ESP32-P4 上

回路从 PC 搬进了芯片。MS2109 采集卡的 UVC 取帧、P4 硬件 JPEG 编解码、TinyUSB 复合 HID 输出、SD 卡存储、C6 上的 ESP-Hosted WiFi、NV3007 状态屏和 WebUI 全部在设备上运行，PC 不再参与回路。

行尾用 `.gitattributes` 固定为 LF，让 SD 卡上的系统提示词在不同检出之间保持逐字节一致——API 的前缀缓存正依赖这一点。

## 2026-05 — Linux/Python 原型（V2.0）

最初的 MVP：回路跑在 PC 上，通过 USB 采集卡和 HID 设备操作目标机器。失败分类学和输入法问题的大部分结论都出自它，它也是这套回路能跑通的第一次证明。保留在 `prototype/` 下作参考，移植到芯片上的代价见 `docs/原型.md`。
